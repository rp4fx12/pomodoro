# pomodoro

This tool is for managing your worktime using the pomodoro technique. This repo also assumes the user has golang installed and is using OSX.

## Setup

1. Clone repo
2. Run `go run main.go`